#include 

int main() {
    /* Given values */
    int a = 5;
    int b = 7;
    int c = 9;

    /* Perform arithmetic operations */
    int sum = a + b + c;

    /* Implement conditional statement */
    if (sum > c) {
        printf("Sum is greater than c\n");
    } else {
        printf("Sum is not greater than c\n");
    }

    return (0);
}
